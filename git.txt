git:

git init  
git config --global user.name "LogeshITHub"
git config --global user.email "logesh24it@gmail.com"
git remote add origin https://github.com/LogeshITHub/Terraform-Practice-2.git
git add . 
git commit -m "Added project files"
git push -u origin master


ignore

folder name ".terraform"

# 1. remove old git history
# 1. Fresh start (Pazhaya history-ah clear panna)
Remove-Item -Recurse -Force .git

# 2. Setup .gitignore (Large files skip panna)
Set-Content .gitignore ".terraform/`n*.tfstate*`n.terraform.lock.hcl"

# 3. Initialize Git
git init
git remote add origin https://github.com/LogeshITHub/Terraform-Practice-2.git
git branch -M main

# 4. Add and Verify (Kandippa check pannunga)
git add .
git ls-files | findstr "terraform-provider-aws" 
# Mela ulla command output empty-ah irukanum!

# 5. Commit and Push
git commit -m "Clean Terraform push - only config files"
git push -u origin main --force