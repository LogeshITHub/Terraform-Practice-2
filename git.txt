git:

git init  
git config --global user.name "LogeshITHub"
git config --global user.email "logesh24it@gmail.com"
git remote add origin https://github.com/LogeshITHub/Terraform-Practice-2.git
git add . 
git commit -m "Added project files"
git push -u origin master


ignore

folder name ".terraform"

# 1. remove old git history
rm -rf .git

# 2. re-init repo
git init
git remote add origin https://github.com/LogeshITHub/Terraform-Practice-2.git

# 3. create .gitignore (VERY IMPORTANT BEFORE add)
echo ".terraform/" >> .gitignore
echo "*.tfstate*" >> .gitignore

# 4. add files (safe)
git add .

# 5. commit
git commit -m "clean terraform repo"

# 6. set branch
git branch -M main

# 7. push (NO force needed)
git push -u origin main